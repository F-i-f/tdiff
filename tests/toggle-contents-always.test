#!/bin/sh

set -eu

. "$srcdir"/tests/contents.lib.sh

tdiff_options="-0w echo ALWAYS RUN %1 VS. %2 ;"
tdiff_long_options="--preset none --exec-always echo ALWAYS RUN %1 VS. %2 ;"

. "$srcdir"/tests/test.lib.sh
