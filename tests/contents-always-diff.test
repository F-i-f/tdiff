#!/bin/sh

set -eu

. "$srcdir"/tests/contents.lib

always_diff_filter() {
  sed -re 's!^([-+]{3} [^[:space:]]+)[[:space:]]+[0-9A-Z].*$!\1!' \
      -e 's!^@@ .* @@$!@@ X @@!' \
      -e '/^No differences encountered$/d'
}

tdiff_options="-AW"
filter=always_diff_filter

. "$srcdir"/tests/test.lib
